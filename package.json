{
    "scripts": {
        "install": "scripts/install.sh",
        "lint": "standard",
        "lint:fix": "standard --fix",
        "logs": "journalctl /usr/bin/gnome-shell -f -o cat",
        "prepack": "scripts/prepack.sh",
        "pack": "gnome-extensions pack . --extra-source lib --force",
        "test": "mocha"
    },
    "devDependencies": {
        "mocha": "^7.1.0",
        "standard": "^14.3.1"
    },
    "standard": {
        "env": {
            "mocha": true
        }
    }
}
