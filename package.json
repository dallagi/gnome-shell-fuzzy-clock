{
    "scripts": {
        "install": "npm run pack && gnome-extensions install Fuzzy_Clock@dallagi.shell-extension.zip --force && echo 'Now restart the shell'",
        "lint": "standard",
        "lint:fix": "standard --fix",
        "logs": "journalctl /usr/bin/gnome-shell -f -o cat",
        "pack": "gnome-extensions pack . --extra-source lib --force",
        "test": "mocha"
    },
    "devDependencies": {
        "mocha": "^7.1.0",
        "standard": "^14.3.1"
    },
    "standard": {
        "env": {
            "mocha": true
        }
    }
}
